const raio = 5.6 // cálculo exponêncial/potência
const area = Math.PI * Math.pow(raio, 2) // que seria raio elevado ao quadrado.

console.log(area)
console.log(typeof Math) // para imprimir o tipo do dado de Match = objeto.
